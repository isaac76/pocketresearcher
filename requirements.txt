# PocketResearcher Requirements
# Core dependencies for local LLM-based mathematical proof exploration

# Transformers and PyTorch for local LLMs
transformers>=4.30.0
torch>=2.0.0

# Optional: Accelerate for faster model loading
accelerate>=0.20.0
